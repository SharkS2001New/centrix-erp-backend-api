; Request / upload limits (Laravel ValidatePostSize reads post_max_size).
; Default PHP Docker image is 8M — too small for logo/product images, imports, and POS outbox sync.
post_max_size = 64M
upload_max_filesize = 64M
max_file_uploads = 50
